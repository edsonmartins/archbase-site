version: '3.9'

x-default-opts:
  &default-opts
  logging:
    options:
      max-size: "10m"

networks:
  traefik-network:
    external: true

services:
  # Frontend - Nginx com Traefik
  frontend:
    <<: *default-opts
    image: archbase-site:latest
    networks:
      - traefik-network
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      update_config:
        parallelism: 1
        delay: 10s
        failure_action: rollback
        order: start-first
      labels:
        # Traefik configuration
        - "traefik.enable=true"
        - "traefik.http.routers.archbase.rule=Host(`archbase.dev`) || Host(`www.archbase.dev`)"
        - "traefik.http.routers.archbase.entrypoints=websecure"
        - "traefik.http.routers.archbase.tls=true"
        - "traefik.http.routers.archbase.tls.certresolver=letsencrypt"
        - "traefik.http.services.archbase.loadbalancer.server.port=80"
        # HTTP to HTTPS redirect
        - "traefik.http.routers.archbase-http.rule=Host(`archbase.dev`) || Host(`www.archbase.dev`)"
        - "traefik.http.routers.archbase-http.entrypoints=web"
        - "traefik.http.routers.archbase-http.middlewares=redirect-to-https"
        - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
